# python.dev-bootstrap

```bash
curl -fsSL 'https://raw.githubusercontent.com/abx-public/dev-bootstrap/refs/heads/main/bootstrap.py?t=0' | python3
```

Pin a release by changing the right-hand side to `env RELEASE_TAG='releases/gh/0.3.9' python3` (same `curl` URL).
